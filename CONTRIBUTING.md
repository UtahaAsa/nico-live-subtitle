# 参与贡献

感谢你愿意改进 Nico Live Subtitle。

## 开发环境

1. 使用 Python 3.10 或更高版本创建虚拟环境。
2. 执行 `python -m pip install -e ".[test]"` 安装开发依赖。
3. 修改前先运行 `python -m unittest discover -s tests -v`。
4. 保持改动聚焦，并为纯逻辑模块补充对应测试。

## 提交问题

请提供 Windows 版本、Python 版本、音频设备类型、识别模型和运行设备。日志中请移除用户名、本地路径及其他隐私信息，不要上传受版权保护的视频或音频片段。

## Pull Request

- 说明问题、改动方式和验证结果。
- 不提交模型文件、虚拟环境、缓存或录制音频。
- 新依赖需要说明体积、许可证与引入原因。

